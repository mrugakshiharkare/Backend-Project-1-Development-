## Flask Regex Matcher – Task 2

#### 📌 `Project Description`
This project is a Flask-based web application that allows users to test
regular expressions against a given input string and view all matched results.

The application demonstrates backend–frontend interaction using Flask,
HTML forms, and Python regular expressions.

---

#### 🛠️ `Technologies Used`
- Python
- Flask
- HTML
- CSS
- Regular Expressions (`re` module)

---

#### ⚙️ `Features`
- User can enter a test string
- User can enter a regular expression
- Displays all matched patterns
- Handles invalid regex patterns gracefully

---

#### 🚀 `How to Run the Project`

```bash
pip install flask
python app.py
```

#### `Open browser and visit:`
http://127.0.0.1:5000/regex

#### 📷 `Output`

- The application displays all matching patterns found in the test string based on the provided regular expression.
- You can refer https://github.com/mrugakshiharkare/Backend-Project-1-Development-/blob/main/Backend-Project-1-Development.png for reference of the output

#### ✨ `Learning Outcome`

- Understanding Flask routing and forms
- Practical usage of regular expressions
- Error handling in Flask applications
